export enum AuthProvider {
  EMAIL = 'email',
  GOOGLE = 'google',
  APPLE = 'apple',
}

export enum SubscriptionTier {
  FREE = 'free',
  PREMIUM = 'premium',
}

export enum SubscriptionStatus {
  ACTIVE = 'active',
  CANCELLED = 'cancelled',
  EXPIRED = 'expired',
}

export enum PreferredLanguages {
  en = 'en',
  es = 'es',
  fr = 'fr',
}

export enum PreferredBibleVersion {
  KJV = 'KJV',
  NIV = 'NIV',
  ESV = 'ESV',
}

export enum PreferredAIVoice {
  FEMALE = 'female',
  MALE = 'male',
}
